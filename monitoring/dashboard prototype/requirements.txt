# Apache Superset environment for dashboard prototype

# Core Superset version (latest stable, 2025)
apache-superset>=3.0.0

# Database drivers (add/remove as needed)

psycopg2-binary>=2.9.9  # PostgreSQL
mysqlclient>=2.2.4      # MySQL/MariaDB
pymssql>=2.3.0          # SQL Server

# For reading logs/reports (custom Python scripts)
pandas>=2.2.0
rich>=13.7.1

# Optional: For automations and API access
requests>=2.31.0
python-dotenv>=1.0.1

# Optional: Task scheduling/automation
celery>=5.4.0

# Optional: LDAP/SSO integration (if needed)
# python-ldap>=3.4.4

# Development
pytest>=8.2.2
